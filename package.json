{
  "name": "cloud-cost-analyzer",
  "version": "1.0.0",
  "description": "Enterprise Multi-Cloud Billing Analytics & Anomaly Detection SDK in TypeScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test tests/*.test.js"
  },
  "author": "Devopstrio <engineering@devopstrio.co.uk>",
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
